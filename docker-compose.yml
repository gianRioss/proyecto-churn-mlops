services:
  churn-api:
    build: .
    container_name: churn_api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./reports:/app/reports
    command: uvicorn src.api:app --host 0.0.0.0 --port 8000